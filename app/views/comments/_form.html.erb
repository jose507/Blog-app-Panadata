<div class="well col-xs-8 col-xs-offset-2">
<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
   
  <div>
  <p> 
      <div class="article-meta-details">
      <small>Usuario: <%= @user.nombre  %>
      </small>
      </div>
    </p>
  </div>
  
    <p>
      <%= form.label :body %><br>
      <%= form.text_area :body %>
    </p>
 
    <p>
      <%= form.submit%>
    </p>
    <center>
    <div class="text-center">
    
    
    <%= link_to 'Editar', edit_article_path(@article), class: "btn btn-primary btn-lg btn-block boton " %> 
    <br>
    
    <%= link_to 'Atrás', articles_path, class: "btn  btn-danger btn-lg btn-block boton responsive" %>
  <% end %>
  
 
  </div>
